# AGENTS.md — greeter spec

This `spec/` directory is the reverse-engineered natural-language specification
for the `greeter` Python package. A coding agent reading these files can
**rebuild `greeter` from scratch** without seeing the original source code.

## What is in this spec

- `README.md` — human overview of the rebuilt project.
- `architecture.md` — goals, constraints, and building blocks.
- `layout/tree.txt` — annotated directory tree of the original.
- `layout/src.map.md` — every file → purpose + public API.
- `specs/` — per-module spec using OpenSpec-lite form
  (Purpose / Requirements / Scenarios).
- `conventions/` — inferred dev rules, each one evidence-backed.
- `inventory/functional-checklist.md` — the grading key for rebuild verification.

## Rebuild instructions (read first)

To rebuild `greeter` from this spec:

1. Read `architecture.md` for goals and constraints.
2. Read `layout/src.map.md` for the target file layout.
3. Read `specs/*.spec.md` for requirements (use SHOULD/MUST).
4. Read `conventions/*.md` for style, tooling, and structure rules.
5. Implement each module. Do **not** copy the original source verbatim —
   reconstruct from the spec.
6. Verify by running through `inventory/functional-checklist.md`. Every entry
   must pass before the rebuild is complete.

## Spec version

v0.1.0 — generated by REgent regent-reverse skill against
publieople/reverse-fixture-tiny @ main.
